{
  "name": "@worldweaver/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "lint": "biome check src package.json tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@worldweaver/config": "workspace:*",
    "@worldweaver/contracts": "workspace:*",
    "fastify": "^5.8.4",
    "zod": "^4.3.6"
  }
}
